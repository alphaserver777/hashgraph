# AI Notes

## Что изменено
- Добавлен ручной bootstrap Skaro для существующего репозитория.
- Созданы проектные артефакты, описывающие стек, архитектуру, инварианты, эксплуатационное состояние и дорожную карту.
- Создана первая milestone/task-ветка и добавлен указатель на новый набор артефактов из README.

## Зачем
- Будущим LLM-сессиям нужен устойчивый и явный контекст, который переживает один README или одну терминальную сессию.
- Skaro опирается на артефакты внутри репозитория, а не только на исходный код.

## Риски
- Эти документы отражают снимок кодовой базы на 2026-03-15 и могут устареть, если будущие изменения не будут обновлять `.skaro/`.
- В `config.yaml` указана общая OpenAI-конфигурация по умолчанию; возможно, ее потребуется подстроить под фактического провайдера команды.

## Проверка
- Структура репозитория сверена с документацией Skaro для импортированных проектов.
- Содержимое артефактов выведено из текущего кода, README и тестов.
