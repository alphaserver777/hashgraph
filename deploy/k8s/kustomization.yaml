apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mdrj

resources:
  - namespace.yaml
  - configmap.yaml
  - services.yaml
  - statefulset.yaml
  # NOTE: secret intentionally NOT in this list. Generate it imperatively:
  #   kubectl -n mdrj create secret generic mdrj-secrets \
  #     --from-literal=hmac-key="$(openssl rand -hex 32)"
