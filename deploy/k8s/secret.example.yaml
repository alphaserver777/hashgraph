# Example only — DO NOT COMMIT real secret with values.
# Generate the real secret at deploy time:
#   kubectl -n mdrj create secret generic mdrj-secrets \
#     --from-literal=hmac-key="$(openssl rand -hex 32)"
apiVersion: v1
kind: Secret
metadata:
  name: mdrj-secrets
  namespace: mdrj
type: Opaque
stringData:
  hmac-key: "replace-me-with-openssl-rand-hex-32"
